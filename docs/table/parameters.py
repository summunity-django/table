parameters = [
    {
        "name": "table",
        "required": False,
        "in": "query",
        "description": "table name",
        "schema": {
            "type": "string"
        }
    },
    {
        "name": "table_id",
        "required": False,
        "in": "query",
        "description": "table index",
        "schema": {
            "type": "string"
        }
    },
]
